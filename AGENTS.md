
## Code memory (Graphify)

Before broad codebase search, prefer `graphify-out/GRAPH_REPORT.md` and
`graphify query` / MCP `query_graph` / `shortest_path` against this repo's
graph. Full graphs also live at
`s3://ivx-graphify-graphs/graphs/<this-repo>/graph.json`.
